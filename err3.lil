; there should be exactly one main function
int main() {
    int k = 5 ; can't initialize variable with declaration
    k = input(k) ; input doesn't accept any params

    array b[(43 + 3)], ; trailing comma
    for (k : b[3] = 5) ; cannot have stmt in rhs
    {}

    return k, k ; return only one value
}

void main() {
    array b[323], c[if(true) bool a] ; no stmt allowed in []
    int k
    k = sizeof b ; paren required around sizeof
    

    return  ;;; 
}